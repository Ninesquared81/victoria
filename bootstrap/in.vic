type c_int := i32

external {
    func printf(fmt: c_string, ..!) -> c_int
}

func main() {
    var a := 5
    var p := &mut a
    p^ := 42
    printf(c"Hello, World!\n")
    printf(c"p = %p, p^ = %lld", p, p^)
}
