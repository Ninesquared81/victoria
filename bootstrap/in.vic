type c_int := i32

external {
    func printf(fmt: c_string, ..!) -> c_int
}

func main() {
    #var a : int
    #var p : ^mut! = &mut a
    var fp: ^func (x: int) = &foo
    var arr: [5]mut int
    arr[2] := 7
    #(p to ^mut int)^ := 42
    #printf(c"Hello, World!\n")
    printf(c"%d\n", arr[2] to c_int)
    (&foo)^(arr[2])
}

func foo(x: int) {

}