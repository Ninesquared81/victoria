type c_int := i32

external {
    func printf(fmt: c_string, ..!) -> c_int
}

func main() {
    var a : int
    var p : ^mut! = &mut a
    var arr: [5]int
    (p to ^mut int)^ := 42
    printf(c"Hello, World!\n")
}
